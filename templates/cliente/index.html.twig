{% extends 'base.html.twig' %}


{% block body %}
<h1>Clientes</h1>
	<ul>
		{% for cliente in listacliente %}
			<li>{{cliente.nombre}} - {{cliente.direccion}} - {{cliente.cp}} - {{cliente.ciudad}}</li>
		{%endfor%}
	</ul>


<p><a href="{{path('cliente_nuevo')}}">Añadir cliente</a></p>

<button type="button" class="btn btn-primary">cambiar color</button>

{% endblock %}

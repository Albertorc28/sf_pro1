{% extends 'base.html.twig' %}


{% block body %}
<h1>Mascotas</h1>
	<ul>
		{% for mascota in listamascota %}
			<li>{{mascota.nombre}} - {{mascota.animal}} - {{mascota.fechanac|date('Y-m-d')}}</li>
		{%endfor%}
	</ul>


<p><a href="{{path('mascota_nuevo')}}">Añadir mascota</a></p>

<button type="button" class="btn btn-primary">cambiar color</button>

{% endblock %}
